
## Start the backend

```bash
# Windows
set FLASK_APP=src/python/app.py && set SECRET_KEY='a-secret' && python -m flask && python -m flask run

# Linux/Mac
export FLASK_APP=src/python/app.py && export SECRET_KEY='a-secret' && python -m flask && python -m flask run
```

